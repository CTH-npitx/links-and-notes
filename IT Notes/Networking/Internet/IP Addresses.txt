Types:

consumer & private
	with a ISP, public is for 
	private is:
	10.0.0.0 - 10.255.255.255
	172.16.0.0 - 172.31.255.255
	192.168.0.0
		most common for consumer
public
	the primary IP for devices that connect directly to the internet. Provided by ISP
dynamic
	change automatically and regulary. That way it's harder to hack, and as it already changes, if you move, it's set up already
static ip
	they are consistant, hardcoded IP's. mostly unused, but necessary for webpages/ mail
shared IP
	normally smaller pages. Typically these sites receive a new address every 7. Can't have 100% uptime as there would be short times where the IP adress
Dedicated IP
	Purchase a specific IP to make it easier to transfer and share files, and make getting a certifice easier

natting: private IP gets put into a new packet with the public IP

most routers default to admin-admin for loging in