Update table
'table' is the name of the table (say, ingredients)
set field1 = value1, field2 = value2, ... fieldn = valuen
where id=x can select row
if pinaple is id 4, and has 2, then:

Update ingredients
SET Qty = 26
where id = 4.

then pinaple's quantity will change to 26

this is just setting, so it overwrites. if decreasing, just overwrite with the calculated value